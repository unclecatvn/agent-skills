[options]
; relative entries: Odoo resolves them against the directory odoo-bin runs in, odoo_trace against
; this file's directory (and says so)
# addons_path = /commented/out
addons_path = addons,../custom
; addons_path = /somewhere/else
db_name = fixture_db
